# FastAPI backend base URL (server-side only, never NEXT_PUBLIC_*)
DOUYIN_API_URL=https://douyin-api.toolnet.tech
# Backend admin token (server-side only, never exposed to browser)
DOUYIN_ADMIN_TOKEN=CHANGE_ME_LONG_RANDOM_TOKEN
# Password for the dashboard login (server-side only)
DASHBOARD_SECRET=CHANGE_ME_DASHBOARD_PASSWORD
